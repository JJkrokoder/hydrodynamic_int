name: hydint_env
channels:
  - conda-forge
  - stochasticHydroTools
  - complexfluidsuam

dependencies:
  - python
  - numpy
  - scipy
  - pyvista
  - libMobility
  - pytest
  - pip
  - uammd-structured=*=*double*
  - pip:  # These are the packages that will be installed with pip
    - pyUAMMD
    - matplotlib
    - pyvista
    - icosphere
    - pyVLMP

  